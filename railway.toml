[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py ensure_admin && python manage.py ensure_fixtures && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
